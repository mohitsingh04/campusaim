<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Campusaim — Confirm Account Deletion</title>
  <style>
    :root {
      --container-width: 760px;
      --bg: #f6f7f9;
      --card-bg: #ffffff;
      --muted: #6b7280;
      --accent: #dc2626;
      /* Red for caution action */
      --highlight: #fde8e8;
      --heading: #2b2b2b;
      --footer: #9aa0a6;
    }

    html,
    body {
      height: 100%;
    }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
        "Helvetica Neue", Arial, sans-serif;
      background: var(--bg);
      color: var(--heading);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      display: flex;
      align-items: flex-start;
      justify-content: center;
      padding: 40px 20px;
    }

    .email-wrap {
      width: 100%;
      max-width: var(--container-width);
      background: var(--card-bg);
      border-radius: 8px;
      padding: 46px 60px;
      box-shadow: 0 1px 0 rgba(0, 0, 0, 0.04),
        0 6px 30px rgba(16, 24, 40, 0.04);
      border: 1px solid rgba(0, 0, 0, 0.03);
    }

    /* --- Logo --- */
    .logo {
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 28px;
    }

    .logo img {
      height: 64px;
      width: auto;
      display: block;
    }

    /* --- Text --- */
    p {
      line-height: 1.65;
      margin: 14px 0;
      color: #111827;
    }

    .greeting {
      margin-top: 6px;
      margin-bottom: 12px;
      color: var(--muted);
    }

    a {
      color: var(--accent);
      text-decoration: underline;
    }

    .hl {
      background: var(--highlight);
      padding: 2px 6px;
      border-radius: 3px;
    }

    .btn {
      display: inline-block;
      background: var(--accent);
      color: #fff !important;
      padding: 10px 22px;
      border-radius: 6px;
      font-weight: 600;
      text-decoration: none;
      margin-top: 16px;
    }

    /* --- Footer --- */
    hr.sep {
      border: 0;
      border-top: 1px solid rgba(0, 0, 0, 0.06);
      margin: 28px 0;
    }

    .footer {
      font-size: 13px;
      color: var(--footer);
      text-align: center;
      padding-bottom: 6px;
    }

    .footer .address {
      font-size: 13px;
      margin-top: 6px;
    }

    @media (max-width: 700px) {
      .email-wrap {
        padding: 28px 20px;
        margin: 0 8px;
      }

      .logo img {
        height: 48px;
      }
    }
  </style>
</head>

<body>
  <article class="email-wrap" role="article" aria-label="Campusaim Account Deletion Confirmation">
    <header class="logo">
      <img src="<%= process.env.API_BASE_URL %>/logo/logo-black.png" alt="Campusaim"
        style="width: auto; height: 35px" />
    </header>

    <section>
      <p class="greeting">Hi,</p>

      <p>
        To verify the email address for your property
        <span class="hl">
          <%= property_name %>
        </span>, please use the One-Time
        Password (OTP) below.
      </p>

      <p>Your verification OTP is:</p>

      <div class="otp-box">
        <%= otp %>
      </div>

      <p>This OTP will expire in <strong>10 minutes</strong>.</p>

      <p>
        If you did not request this verification, you can ignore this email
        safely.
      </p>

      <p class="signature">
        Thank you for helping us keep your property information secure 🔐
      </p>

      <p class="signature">Warm regards,<br />Team Campusaim</p>
      <div style="display: flex; gap: 14px">
        <a href="https://x.com/PrernaYog40326">
          <img src="<%= process.env.API_BASE_URL %>/social-links/x-twiter.webp" alt="Twitter-X" style="
                height: 26px;
                width: auto;
                background: white;
                display: inline-block;
                margin-left: 14px;
              " />
        </a>
        <a href="https://www.linkedin.com/company/yogprerna">
          <img src="<%= process.env.API_BASE_URL %>/social-links/linkedin.webp" alt="LinkedIn" style="
                height: 26px;
                width: auto;
                background: white;
                display: inline-block;
                margin-left: 14px;
              " />
        </a>
        <a href="https://www.youtube.com/@yogprerna108">
          <img src="<%= process.env.API_BASE_URL %>/social-links/youtube.webp" alt="YouTube" style="
                height: 26px;
                width: auto;
                background: white;
                display: inline-block;
                margin-left: 14px;
              " />
        </a>
        <a href="https://www.instagram.com/yog_prerna/">
          <img src="<%= process.env.API_BASE_URL %>/social-links/insta.webp" alt="Instagram" style="
                height: 26px;
                width: auto;
                background: white;
                display: inline-block;
                margin-left: 14px;
              " />
        </a>
        <a href="https://www.facebook.com/yogprernaofficial">
          <img src="<%= process.env.API_BASE_URL %>/social-links/facebook.webp" style="
                height: 26px;
                width: auto;
                background: white;
                display: inline-block;
                margin-left: 14px;
              " alt="Facebook" />
        </a>
      </div>

      <hr class="sep" />

      <div class="footer">
        <div>© <%= new Date().getFullYear() %> Campusaim AI</div>
        <div class="address">Campusaim, Dehradun, 248001</div>
      </div>
    </section>
  </article>
</body>

</html>